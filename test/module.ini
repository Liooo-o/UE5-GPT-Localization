needGPT = true

[map]
battle=battle,ability

[array]
weight = battle

; 举例:
; 等号左边是模块名，等号右边是 tab 文件的后缀名，common 不用填
; [map]
; battle=battle,ability
; lobby=lobby1,lobby2
; room=room1,room2
; 
; 模块的权重，在等号右边，填上模块名，按顺序来，越后面的权重越大，common模块不用填
; [array]
; weight = battle,lobby,room
